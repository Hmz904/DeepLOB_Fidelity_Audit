protocol: author_pytorch_corrected
run_name: fi2010-author-pytorch-corrected

data:
  dataset: fi2010
  raw_dir: data/raw
  processed_dir: data/processed
  horizons: [10, 20, 50, 100]

models:
  names: [deeplob]

training:
  seed: 42
  device: auto
